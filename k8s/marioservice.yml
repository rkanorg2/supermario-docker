apiVersion: v1
kind: Service
metadata:
  name: web-service-mario
spec:
  type: LoadBalancer
  selector:
    app: mario
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
